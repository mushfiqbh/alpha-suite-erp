import 'package:flutter/material.dart';

class HrView extends StatelessWidget {
  const HrView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('HR Module'));
  }
}
